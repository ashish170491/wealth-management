<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>3.4.0</version>
		<relativePath/> <!-- lookup parent from repository -->
	</parent>
	<groupId>com.example</groupId>
	<artifactId>intraday-app</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name>intraday-app</name>
	<description>Automated Stock Trading System</description>
	<properties>
		<java.version>21</java.version>
		<!-- B-085. Without this javac reads sources in the platform default (windows-1252
		     here) while every file on disk is UTF-8, so any non-ASCII character in a STRING
		     LITERAL is compiled to U+FFFD. It is not cosmetic and it is not confined to
		     comments: 89 literals are affected, including email subject lines and the arrows,
		     bullets and rupee symbols in report bodies. Caught when a new endpoint's em dashes
		     came back as replacement characters over HTTP. All 304 sources verified valid
		     UTF-8 before this was set, so nothing is re-interpreted by turning it on. -->
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
		<!--
		  Spring AI (SPEC 48). The 1.1.x line supports Spring Boot 3.4.x and 3.5.x; 2.0.x requires
		  Boot 4.0/4.1 (Jackson 3) and must NOT be used here. GA artifacts are on Maven Central, so
		  no milestone or snapshot repository is needed.
		-->
		<spring-ai.version>1.1.8</spring-ai.version>
	</properties>
	<dependencyManagement>
		<dependencies>
			<dependency>
				<groupId>org.springframework.ai</groupId>
				<artifactId>spring-ai-bom</artifactId>
				<version>${spring-ai.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
		</dependencies>
	</dependencyManagement>
	<dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webflux</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-configuration-processor</artifactId>
			<optional>true</optional>
		</dependency>
		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok</artifactId>
			<optional>true</optional>
		</dependency>
		<dependency>
			<groupId>com.h2database</groupId>
			<artifactId>h2</artifactId>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<groupId>org.postgresql</groupId>
			<artifactId>postgresql</artifactId>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<groupId>org.ta4j</groupId>
			<artifactId>ta4j-core</artifactId>
			<version>0.16</version>
		</dependency>
		<dependency>
			<groupId>org.springframework.retry</groupId>
			<artifactId>spring-retry</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework</groupId>
			<artifactId>spring-aspects</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-mail</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>com.warrenstrange</groupId>
			<artifactId>googleauth</artifactId>
			<version>1.5.0</version>
		</dependency>
		<dependency>
			<groupId>commons-codec</groupId>
			<artifactId>commons-codec</artifactId>
		</dependency>
		<dependency>
			<groupId>io.projectreactor</groupId>
			<artifactId>reactor-test</artifactId>
			<scope>test</scope>
		</dependency>

		<!--
		  PDF text extraction for earnings-call transcripts (SPEC 34, F8).
		  Transcripts are mandatory exchange filings under SEBI LODR Reg 46 and arrive as
		  PDF attachments on the corporate-announcements feed we already fetch, so this is
		  the only piece missing to read them. Text extraction only - no rendering, no
		  fonts, no image codecs.
		-->
		<dependency>
			<groupId>org.apache.pdfbox</groupId>
			<artifactId>pdfbox</artifactId>
			<version>3.0.3</version>
		</dependency>

		<!--
		  Spring AI chat starters (SPEC 48). Both are on the classpath so the macro event extractor
		  is model-agnostic: `spring.ai.model.chat` picks one, and `none` (the default) leaves NO
		  ChatModel bean at all, which is what makes the keyword extractor the honest fallback
		  rather than a crash.

		  IMPORTANT: each provider's auto-configuration is match-if-missing, so with both starters
		  present and the property absent, BOTH ChatModel beans are created and injection is
		  ambiguous. The property must always be set explicitly - see application.yml.

		  The OpenAI starter also serves any OpenAI-compatible endpoint via `spring.ai.openai.base-url`,
		  so a local or third-party model needs no new dependency.
		-->
		<dependency>
			<groupId>org.springframework.ai</groupId>
			<artifactId>spring-ai-starter-model-openai</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.ai</groupId>
			<artifactId>spring-ai-starter-model-anthropic</artifactId>
		</dependency>

		<!-- Jackson for JSON model parsing (already in spring-boot-starter-web) -->
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
				<configuration>
					<excludes>
						<exclude>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
						</exclude>
					</excludes>
				</configuration>
			</plugin>
		</plugins>
	</build>

</project>
